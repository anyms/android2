package app.spidy.pirum.utils

object PlaylistStatus {
    const val STATE_STOPPED = DownloadStatus.STATE_STOPPED
    const val STATE_COMPLETED = DownloadStatus.STATE_COMPLETED
    const val STATE_NONE = DownloadStatus.STATE_NONE
    const val STATE_DOWNLOADING = DownloadStatus.STATE_DOWNLOADING
}